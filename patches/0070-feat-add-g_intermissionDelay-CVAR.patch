From a4f59c8458c5d454f9f16a14cd9bf491c7a64752 Mon Sep 17 00:00:00 2001
From: WofWca <wofwca@protonmail.com>
Date: Mon, 6 Jul 2026 15:20:16 +0400
Subject: [PATCH] feat: add `g_intermissionDelay` CVAR

---
 code/game/g_cvar.h  | 5 +++++
 code/game/g_local.h | 5 +----
 code/game/g_main.c  | 6 ++++--
 3 files changed, 10 insertions(+), 6 deletions(-)

diff --git a/code/game/g_cvar.h b/code/game/g_cvar.h
index d4d963a62..aabdbbe5c 100644
--- a/code/game/g_cvar.h
+++ b/code/game/g_cvar.h
@@ -135,6 +135,11 @@ G_CVAR( g_gibsOnCollisionAffectLivePlayers, "g_gibsOnCollisionAffectLivePlayers"
 	"This one affects gameplay significantly, and is basically just for fun." )
 G_CVAR( g_gibsOnCollisionInheritPlayerVelocity, "g_gibsOnCollisionInheritPlayerVelocity", "1.0", CVAR_ARCHIVE, NO_TRACK, NULL )
 
+G_CVAR( g_intermissionDelay, "g_intermissionDelay", "1000", NO_FLAGS, TRACK, NULL )
+#ifdef MISSIONPACK
+G_CVAR( g_intermissionDelaySinglePlayer, "g_intermissionDelaySinglePlayer", "5000", NO_FLAGS, TRACK, NULL )
+#endif
+
 G_CVAR( g_podiumDist, "g_podiumDist", "80", NO_FLAGS, NO_TRACK, NULL )
 G_CVAR( g_podiumDrop, "g_podiumDrop", "70", NO_FLAGS, NO_TRACK, NULL )
 
diff --git a/code/game/g_local.h b/code/game/g_local.h
index 1092726a5..bdf2a106f 100644
--- a/code/game/g_local.h
+++ b/code/game/g_local.h
@@ -39,9 +39,6 @@ Foundation, Inc., 51 Franklin St, Fifth Floor, Boston, MA  02110-1301  USA
 #define	CARNAGE_REWARD_TIME	3000
 #define REWARD_SPRITE_TIME	2000
 
-#define	INTERMISSION_DELAY_TIME	1000
-#define	SP_INTERMISSION_DELAY_TIME	5000
-
 // gentity->flags
 #define	FL_GODMODE				0x00000010
 #define	FL_NOTARGET				0x00000020
@@ -402,7 +399,7 @@ typedef struct {
 
 	// intermission state
 	int			intermissionQueued;		// intermission was qualified, but
-										// wait INTERMISSION_DELAY_TIME before
+										// wait g_intermissionDelay before
 										// actually going there so the last
 										// frag can be watched.  Disable future
 										// kills during this delay
diff --git a/code/game/g_main.c b/code/game/g_main.c
index da9bfc106..a58fb4719 100644
--- a/code/game/g_main.c
+++ b/code/game/g_main.c
@@ -1218,13 +1218,15 @@ void CheckExitRules( void ) {
 
 	if ( level.intermissionQueued ) {
 #ifdef MISSIONPACK
-		int time = (g_singlePlayer.integer) ? SP_INTERMISSION_DELAY_TIME : INTERMISSION_DELAY_TIME;
+		int time = (g_singlePlayer.integer)
+			? g_intermissionDelaySinglePlayer.integer
+			: g_intermissionDelay.integer;
 		if ( level.time - level.intermissionQueued >= time ) {
 			level.intermissionQueued = 0;
 			BeginIntermission();
 		}
 #else
-		if ( level.time - level.intermissionQueued >= INTERMISSION_DELAY_TIME ) {
+		if ( level.time - level.intermissionQueued >= g_intermissionDelay.integer ) {
 			level.intermissionQueued = 0;
 			BeginIntermission();
 		}
-- 
2.55.0

