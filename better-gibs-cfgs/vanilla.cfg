set cg_gibs 1.0
set cg_oldGibs 1
// Have to set explicitly in case we are not the server,
// because `cg_oldGibs 1` doesn't affect this.
// See https://github.com/WofWca/quake3-better-gibs-mod/issues/14.
set cg_gibsBetterCameraOnGib 0

set g_oldGibs 1
